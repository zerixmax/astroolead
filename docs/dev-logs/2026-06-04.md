# Development Log - 2026-06-04

## Verzija: 0.3.0 (SEO and Sitemap Localization Release)

### 🛠️ Odrađeni zadaci
* **Nove rute s engleskim nazivima (Lokalizacija ruta):**
  - Uklonjen je dinamički direktorij `src/pages/[lang]` koji je uzrokovao poteškoće s preusmjeravanjem i i18n translacijom ruta na statičkoj razini.
  - Kreirane su eksplicitne jezične staze: `/hr/o-projektu` (za hrvatski) i `/en/about-project` (za engleski).
  - Ostale podstranice (`oleadbot.astro`, `oleadenode.astro`, `nft.astro`) su također premještene u odgovarajuće `/hr/` i `/en/` direktorije kao zasebne statičke stranice.
* **Integracija sitemapa (`@astrojs/sitemap`):**
  - Uspješno instaliran i registriran `@astrojs/sitemap` u `astro.config.mjs`.
  - Definirana `site` adresa (`https://oleadigitalis.eu`) na kojoj se generiraju `sitemap-index.xml` i `sitemap-0.xml`.
  - Sitemap ispravno mapira sve statičke lokalizirane putanje (npr. `/hr/o-projektu/`, `/en/about-project/`).
* **Multilingualni SEO i Hreflang Alternates:**
  - Ažurirana `src/layouts/Layout.astro` komponenta koja dinamički mapira i generira kanonske adrese i `link rel="alternate" hreflang="..."` tagove za svaku stranicu.
  - Ako se korisnik nalazi na `/hr/o-projektu/`, hreflang alternate za `en` ispravno pokazuje na `/en/about-project/` i obratno.
* **Unifikacija preklopnika jezika u Navbaru:**
  - Preuređena logika jezičnog preklopnika u `Navbar.astro` (i za desktop i za mobilne uređaje) tako da umjesto preusmjeravanja na početnu stranicu preusmjerava na ekvivalentnu stranicu drugog jezika (npr. prebacivanje s `/hr/o-projektu` vodi izravno na `/en/about-project`).
* **Uvođenje rječnika putanja (`routes.ts`):**
  - Kreirana je datoteka `src/i18n/routes.ts` u kojoj je definiran rječnik putanja za prevođenje slugova (npr. mapiranje hrvatskog `o-projektu` na engleski `about-project`).
  - Integriran `routes` rječnik u `Navbar.astro` i `Layout.astro` za robusno i automatizirano pronalaženje i generiranje alternates/hreflang poveznica, izbjegavajući ad-hoc string zamjene.
* **Vraćanje Hero pozadinske slike:**
  - Vraćena je prethodna pozadinska slika `olive-hero-bg.webp` u `Hero.astro` umjesto `olead-digitalis-astro-202604.webp` po zahtjevu korisnika.
* **Verzija:** Ažuriran `src/data/version.json` na verziju `0.3.0` s datumom `2026-06-04`.

### 🚧 Poznati problemi (Bugs)
* Nema. Build je uspješno verificiran i prolazi bez grešaka.

### 🎯 Sljedeći koraci
* [ ] Implementacija animacija ulaska za elemente na početnoj stranici (scroll reveal).
* [ ] Integracija CI/CD pipelinea preko GitHub Actions.

### 🛡️ Senior Check
- [x] Tailwind klase su poštovane (nema hardkodiranih boja).
- [x] Provjereno na Mobile i Desktop prikazu.
- [x] Nije prekršena struktura u `tailwind.config.mjs`.
- [x] Ažurirana je verzija u `src/data/version.json` u skladu s novim buildom.

---

## Session 2 — Bug Fix Release

### 🐛 Popravci grešaka (Bug Fixes)

* **🔴 CSS color mismatch (`global.css` vs `branding.md`):**
  - `src/styles/global.css` je imao pogrešne vrijednosti boja u `@theme` bloku: `#f4f4f0` umjesto `#F4F1EA` (olea-sand), `#3f4e35` umjesto `#556B2F` (olea-olive), `#2a2a2a` umjesto `#3D2B1F` (olea-earth).
  - Budući da Tailwind v4 `@theme` u CSS-u override-a JS config, stranica se renderirala s krivim tamnijim bojama.
  - **Popravak:** Vraćene canonical vrijednosti iz `branding.md` u `src/styles/global.css`.

* **🟡 Root ruta `/` conflict — build warning:**
  - `src/pages/index.astro` je bio prazan (samo HTML komentari) i konfliktirao s Astro i18n redirectom (`redirectToDefaultLocale: true`).
  - Build je prošao uz warning: *"Could not render `` from route `/` as it conflicts with higher priority route `/`."*
  - **Popravak:** Obrisan prazni `index.astro`, ali Astro zahtijeva root index za i18n → vraćen minimalni `src/pages/index.astro` s praznim frontmatterom.

* **🟡 Navbar EN link za "O projektu" vodi na krivi URL:**
  - Desktop i mobilni nav linkovi su koristili `getRelativeLocaleUrl(lang, 'o-projektu')` za oba jezika.
  - Na EN siteu ruta je `/en/about-project/`, a `getRelativeLocaleUrl('en', 'o-projektu')` vraća `/en/o-projektu/` (404).
  - **Popravak:** Zamijenjeno s `getRelativeLocaleUrl(lang, routes[lang]['o-projektu'])` u `src/components/ui/Navbar.astro:58,119`.

* **🟢 Hardcodirani tekstovi prebačeni u i18n sustav:**
  - `o-projektu.astro` i `about-project.astro`: Sekcijski naslovi ("Povijest i korijeni", "Blockchain kao rješenje", "Primjeri iz svijeta", "Vizija Budućnosti", CTA sekcija) i labeli ("Vanjski izvor", "Posjeti", "Započnimo suradnju") — prebačeni u `hr.json`/`en.json` pod `o_projektu.*`.
  - `nft.astro` (HR i EN): Svi `isHr ? "..." : "..."` ternari ("Prvi ASA NFT Certifikat", "Registar aktivnih certifikata", "MREŽA AKTIVNA", "POTVRĐENO", itd.) — prebačeni u JSON pod `certifikati.*`.

* **🟢 SEO title za o-projektu koristi i18n:**
  - Layout title je bio hardcodan kao `isHr ? "O projektu | Olea Digitalis" : "About the Project | Olea Digitalis"`.
  - **Popravak:** Sada koristi `t('seo.oProjektuTitle')` u obje verzije (`hr/o-projektu.astro`, `en/about-project.astro`).

### 📁 Izmijenjene datoteke
| Datoteka | Promjena |
|----------|----------|
| `src/styles/global.css` | Ispravljene boje u `@theme` bloku |
| `src/pages/index.astro` | Obrisan, pa vraćen minimalni redirect |
| `src/components/ui/Navbar.astro` | `o-projektu` link koristi `routes` dictionary |
| `src/pages/hr/o-projektu.astro` | Uklonjeni ternari, korišten `content.*` i `t()` |
| `src/pages/en/about-project.astro` | Uklonjeni ternari, korišten `content.*` i `t()` |
| `src/pages/hr/nft.astro` | Uklonjeni ternari, korišten `content.*` |
| `src/pages/en/nft.astro` | Uklonjeni ternari, korišten `content.*` |
| `src/data/i18n/hr.json` | Dodani ključevi za `o_projektu.*` i `certifikati.*` |
| `src/data/i18n/en.json` | Dodani ključevi za `o_projektu.*` i `certifikati.*` |

### ✅ Build Status
- Build prolazi: **11 stranica, 2.66s, 0 grešaka.**
- Preostaje 1 harmless warning: root `/` route conflict (poznato Astro i18n ponašanje).

### 🧹 Preostalo (neadresirano)
- `Slideshow.astro` — mrtav kod (nikad uvezen ni korišten)
- ~7 neiskorištenih slika u `src/assets/images/`
- `tailwind.config.mjs` se može očistiti (Tailwind v4 koristi `@theme` u CSS-u, JS config je redundantan)
